name: Build, Scan, Push Docker Images

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  DOCKER_REGISTRY: docker.io
  FRONTEND_IMAGE: ${{ secrets.DOCKER_USERNAME }}/frontend-app
  BACKEND_IMAGE: ${{ secrets.DOCKER_USERNAME }}/backend-api

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      username: ${{ secrets.DOCKER_USERNAME }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

  build-frontend:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build frontend Docker image
        run: docker build -t ${{ env.FRONTEND_IMAGE }}:latest ./frontend

      - name: Trivy scan frontend image
        id: trivy_frontend
        continue-on-error: true
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.FRONTEND_IMAGE }}:latest
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH'
          skip-files: |
            root/.npm/_cacache/**

      - name: Push frontend image
        if: always()
        run: docker push ${{ env.FRONTEND_IMAGE }}:latest

  build-backend:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build backend Docker image
        run: docker build -t ${{ env.BACKEND_IMAGE }}:latest ./backend

      - name: Trivy scan backend image
        id: trivy_backend
        continue-on-error: true
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.BACKEND_IMAGE }}:latest
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH'
          skip-files: |
            root/.npm/_cacache/**

      - name: Push backend image
        if: always()
        run: docker push ${{ env.BACKEND_IMAGE }}:latest

  bom-frontend:
    needs: build-frontend
    runs-on: ubuntu-latest
    steps:
      - name: Generate SBOM for frontend image (CycloneDX)
        run: |
          docker pull ${{ env.FRONTEND_IMAGE }}:latest
          trivy image --format cyclonedx --output frontend-sbom.xml ${{ env.FRONTEND_IMAGE }}:latest

      - name: Upload frontend SBOM
        uses: actions/upload-artifact@v4  # ✅ Fixed version
        with:
          name: frontend-sbom
          path: frontend-sbom.xml

  bom-backend:
    needs: build-backend
    runs-on: ubuntu-latest
    steps:
      - name: Generate SBOM for backend image (CycloneDX)
        run: |
          docker pull ${{ env.BACKEND_IMAGE }}:latest
          trivy image --format cyclonedx --output backend-sbom.xml ${{ env.BACKEND_IMAGE }}:latest

      - name: Upload backend SBOM
        uses: actions/upload-artifact@v4  # ✅ Fixed version
        with:
          name: backend-sbom
          path: backend-sbom.xml

